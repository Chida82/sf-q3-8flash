# Tasks

## 1. Branch

- [x] 1.1 From a clean `main`, run `git config rerere.enabled true` and `git switch -c perf/bench-harness`. Verify `git status --short --branch` shows the new branch with no changes.
- [x] 1.2 Situation 0 (design D13): with nothing else resident, from the StarForge checkout run `tools/parity-check.sh sf-q3-8flash "<child>/qwen3.8-flash-next.gguf"` on the fresh branch (identical to `main`). Verify it prints `PARITY OK (10 prompts)` with ten `[ok ]` lines. If any prompt shows `[DIFF]`, `[FAIL]` or a speed trip, stop and ask: `main` is not a valid reference.

## 2. Bench: MTP mode (`ds4_bench.c`)

- [x] 2.1 Add `bool mtp` to `bench_config`, parse `--mtp`, and pass it as `.glm_mtp` in `ds4_engine_options`. In `parse_options`, reject `--mtp` together with `--teacher-forced-decode` with exit 2 (like the other option checks there). Verify model-less: `./sf-q3-8flash-bench --mtp --teacher-forced-decode --prompt-file rax.c; echo $?` prints the error and `2`.
- [x] 2.2 Replace `const bool speculative = false; /* sf-ablate(specdec) ... */` with `cfg.mtp && ds4_engine_mtp_draft_tokens(engine) > 1`, and when `cfg.mtp && !speculative` print `ds4-bench: --mtp did not enable built-in MTP (CPU backend or no MTP layer)` and exit 1, freeing what upstream's DSpark check frees. Change the two loop messages `DSpark decode at frontier` to `MTP decode at frontier`. Verify `rg -n 'sf-ablate\(specdec\)|DSpark' ds4_bench.c` prints nothing, and read that the new error path frees the CSV file, session, prompt and engine in the same order as the other early exits in `main` (not exercised on the CPU backend: that would open the 70 GiB model on CPU).
- [x] 2.3 In MTP mode, count per frontier: calls, committed tokens, and for k = 1..3 the calls that committed k tokens and their summed time (the loop already has `token_t0`/`token_t1` and `cycle_tokens`). After the frontier's decode, print `ds4-bench: mtp[ctx=N] cycles=C tokens=T k1=n/ms k2=n/ms k3=n/ms` (ms = mean per call) to stderr. Leave the `DS4_BENCH_SPEC_TRACE` block as it is. Verified by the model-backed smoke test (4.2).

## 3. Bench: frontiers and evidence (`ds4_bench.c`, `ds4_help.c`)

- [x] 3.1 Parse `--frontiers N1,N2,...` into a strictly increasing list of positive ints (at most 64 entries), else exit 2 naming the option. Set `ctx_start` and `ctx_max` to its first and last entry before the existing checks, and walk the list instead of `next_frontier` when it is set. Add `--frontiers LIST` to the bench block of `ds4_help.c`. Verify model-less: `--frontiers 2048,512` and `--frontiers 0,8` both exit 2; `./sf-q3-8flash-bench --help | grep -c -- --frontiers` prints 1.
- [x] 3.2 In the `--show-output` block, after the decoded text, print `ds4-bench: gen[ctx=N] token ids: <id> <id> ...` from `gen_token_buf`. Verified by 4.1.
- [x] 3.3 Give `write_frontier_logits_json` a filename suffix argument; keep the existing call as `frontier_%06d.logits.json` and add a call after the generation loop, before the restore, writing `frontier_%06d.decode.logits.json`. Verified by 4.1.
- [x] 3.4 Run `make -j8` and confirm it prints no warnings, then `make test -j8` and read that its output names `ds4_test --server`, `test_layer_pack`, `test_prompt_prefix`, `test_sampling`, `test_image_decode` and `test_model_download.py` as run and passing (AGENTS rule 19: the exit code alone is not the gate).

## 4. Bench smoke test on the local GGUF

- [x] 4.1 With nothing else resident, run plain: `./sf-q3-8flash-bench -m "$PWD/qwen3.8-flash-next.gguf" --prompt-file speed-bench/promessi_sposi.txt --frontiers 8192,8704,10752 -n 32 --show-output --dump-frontier-logits-dir $TMPDIR/sf-ab-smoke --csv $TMPDIR/sf-ab-smoke.csv`. Verify: the CSV has 3 rows with `prefill_tokens` 8192, 512, 2048; stderr has 3 `token ids` lines of 32 ids; the directory has 3 `.logits.json` and 3 `.decode.logits.json` files. Note whether stderr says it is replaying prefixes (snapshot above 1 GiB); if so, record it in design.md Risks.
- [x] 4.2 Run MTP: the same with `--mtp --prompt-file rax.c --frontiers 2048 -n 128`. Verify one `mtp[ctx=2048]` line whose `tokens` equals the CSV `gen_tokens` (or is lower only because EOS ended the frontier) and whose k1..k3 counts sum to `cycles`.
- [x] 4.3 Run the plain command twice more with the same arguments and compare: the token-id lines and all six logits files must be identical across the runs (`cmp`). If they are not, stop: the baseline is nondeterministic and the gate design needs revisiting before the harness is built.

## 5. Harness (`speed-bench/ab_bench.py`)

- [x] 5.1 CLI and inputs: `--a DIR --b DIR`, `-m GGUF`, `--kinds` (default `plain,mtp-code,mtp-prose`), `--budget SEC` (default 480, refuse above 600), `--bitwise`, `--env KEY=VALUE` (repeatable), `--max-gpu-temp` (60), `--preheat` (210 s), `--min-freq-of-median` (0.90), `--out DIR` (default `$TMPDIR/sf-q3-8flash-ab/<UTC timestamp>`). Validate each tree (has `Makefile` and `metal/`), run `make -C DIR sf-q3-8flash-bench`, record short HEAD and dirty state. Resolve the model and the two prompts (D11) to absolute paths from the script's own checkout. Build the child env without any inherited `DS4_*` plus the `--env` pairs. Verify: `python3 speed-bench/ab_bench.py --a /tmp --b .` exits 2 naming `/tmp`; `--budget 601` exits 2.
- [x] 5.2 Preflight (D9): one `mactop --headless --count 1` JSON sample for AC power, thermal state and GPU temperature; a non-blocking `fcntl.flock` probe of `/tmp/sf-q3-8flash.lock`; `ps -axo pid=,rss=,comm=` for any other process at or above 8 GiB. Each failure prints its condition and exits 2; a missing `mactop` says it is required. Verify on the machine: `--max-gpu-temp 1` exits 2 naming GPU temperature, and holding the lock from another shell (`python3 -c 'import fcntl,time;f=open("/tmp/sf-q3-8flash.lock","a");fcntl.flock(f,fcntl.LOCK_EX);time.sleep(60)'`) makes it exit 2 naming the lock.
- [x] 5.3 GPU monitor (D10): start `mactop --headless --interval 1000 --count 0` writing `gpu.json` (one JSON object per line; the CSV format has no GPU power) after preflight, stop it at exit (also on error), abort if it exits early, and look up the frequency, activity, power, temperature and thermal-state keys, aborting with the mactop version if one is missing. Verify: after a harness run, `gpu.json` has about one line per second of the run and every key the summary uses is present.
- [x] 5.4 Runs: one function that runs a (build, kind) with `cwd` set to the build's tree, the command lines of D4/D7 (`--show-output` always, logits dir only in bitwise warm-up), stderr saved to `logs/<n>-<build>-<kind>.err`, and wall-clock start/end recorded. Parse the bench CSV, the `token ids` lines and the `mtp[...]` lines into per-frontier samples. Unit-covered in 6.1; end-to-end in 7.1.
- [x] 5.5 Schedule (D7): warm-up (A then B for each kind), correctness check on the warm-up and exit 1 on mismatch; preheat runs alternating A and B on the first kind until `--preheat` seconds after the first run started, at most half the budget (D10); then quads A B B A per kind, starting a quad only when its prediction from the latest measured (build, kind) durations plus 10% fits the remaining budget. Unit-covered in 6.1.
- [x] 5.6 Correctness (spec "Correctness gate"): compare token hashes of every run against the first A run of the same kind and frontier, reporting kind, frontier and first differing position, and a nondeterministic baseline when two A runs differ. In bitwise mode compare every prefill and decode dump of the A and B warm-up through `validate_dump` of `gguf-tools/quality-testing/compare_frontier_logits.py` (D6), reporting frontier and vocabulary index. Unit-covered in 6.1.
- [x] 5.7 Pairs and verdict (D10, spec "Interleaved timed rounds"): per-run active-GPU median frequency, temperature, power and thermal states, `unjudged` below two active lines; thermal state reported, never a reason to drop; pairs (A1,B1) and (B2,A2), a pair dropped when one of its runs is below `--min-freq-of-median` of the timed runs' median frequency; per metric the A and B medians, median B/A, ratio range and n; inconclusive (exit 3) when any headline metric has fewer than two pairs. Write `samples.csv` and `summary.txt` (spec "Output") and print the summary. Unit-covered in 6.1.
- [x] 5.8 End the summary with the record row (spec "Record row"): date, B's short commit, model file name, valid pairs, correctness, and for each metric B's median with the median B/A ratio (`54.1 (+3.2%)`); a metric not run gets an empty cell, so the column count never changes. Unit-covered in 6.1.

## 6. Harness test

- [x] 6.1 Add `tests/test_ab_bench.py` (unittest, loading the script like `tests/test_serve_concurrency_bench.py` does) with synthetic inputs: bench CSV plus stderr parsing (token ids and `mtp[...]` lines); pairing, median and ratio range; a dropped pair leaving the other; a disturbed run dropping its pair (the third A/A's frequencies) while plateau runs are kept; the inconclusive rule; budget prediction stopping before an overrunning quad; a token mismatch reporting its first position; a nondeterministic baseline; a one-bit logit difference (`"1.00000012"` vs `"1.00000024"`) and `"-0"` vs `"0"` both failing bitwise; the mactop JSON stream with a partial last line and a missing key aborting; preheat until its deadline; inherited `DS4_*` stripped; the record row having the same number of cells with all kinds and with `--kinds plain`. Verify `python3 tests/test_ab_bench.py -v` lists every test as `ok`.

## 7. Validation on the target machine

- [x] 7.1 A/A run: `python3 speed-bench/ab_bench.py --a . --b . --bitwise` on the Q4 GGUF, charger connected. Verify: exit 0, correctness passes (tokens and bitwise), total wall time within the budget including warm-up and preheat, every headline metric has at least two valid pairs, and every median B/A lies within the ratio ranges' noise (record the ranges). Record the cold and warm first-run duration; if the default budget fits fewer than two quad sets, lower the per-kind `--gen-tokens` defaults or raise the default budget (never above 600 s) and re-run.
- [x] 7.2 Negative gate: `git worktree add $TMPDIR/sf-ab-neg HEAD`, perturb one arithmetic constant in a `metal/qwen4.metal` kernel that the plain run executes (read its call site in `ds4_metal.m` first), and run `--a . --b $TMPDIR/sf-ab-neg --kinds plain --bitwise`. Verify exit 1 with a bitwise failure naming a frontier, which also shows that B ran its own kernels (D1). Then `git worktree remove --force $TMPDIR/sf-ab-neg`.
- [x] 7.3 MTP depth split for `30-mtp-cycle`: run `--kinds mtp-code --env DS4_QWEN4_MTP_DEPTH=2` and then with `=3`. Verify the summaries list the variable and that the k3 count is 0 at depth 2 and non-zero at depth 3.
- [x] 7.4 Q2 model: if `Qwen3.8-Flash-Next-Q2.gguf` is present locally, run `--a . --b . -m <abs path> --kinds plain` and verify exit 0 and that the summary names the Q2 file. If it is absent, do not download it (137+ GiB rule); note in the PR that this check waits for the download.

## 8. Docs, record and close

- [x] 8.1 Add a short "A/B harness" section to `speed-bench/README.md`: the one command, the tree-and-cwd rule (kernels load from `metal/` in the working directory), preflight conditions, default budget and kinds, exit statuses, the A/A noise floor and threshold values from 7.1, and the references table of design D13 (quality against upstream with `parity-check.sh`; bitwise and speed against `main` or the previous step with `ab_bench.py`). In `docs/PERFORMANCE.md`, replace the single bench command with a pointer to that section for before/after verdicts, keeping the parity paragraph. Verify both files mention `ab_bench.py` and nothing in them describes a flag the bench or the harness does not have (`--mtp-timing` is not a bench flag).
- [x] 8.2 Create `speed-bench/perf-record.md` (design D14): what it answers, the column header matching the harness's record row, the procedure for a row (A = worktree of the segment's start commit, B = the change's final tree, `parity-check.sh` passing first), and the segment rule after a sync that changes greedy output. Leave the start row to the first performance change after this one (design D14). Verify the header has the same number of cells as a record row printed in 7.1.
- [x] 8.3 Add to the `context` of `openspec/config.yaml`: every performance change ends with `tools/parity-check.sh sf-q3-8flash` passing and an A/B run of `speed-bench/ab_bench.py` against the start commit of the current segment of `speed-bench/perf-record.md`, and appends the printed row there. Verify with `openspec instructions tasks --change 20-perf-bench-harness --json | python3 -c 'import json,sys; print(json.load(sys.stdin)["context"])'`, which should print the new rule.
- [x] 8.4 Closing parity: the same command as 1.2 on the finished branch. Verify `PARITY OK (10 prompts)`; this also shows the bench changes did not touch CLI output.
- [x] 8.5 Check scope: `git status --short` lists only `ds4_bench.c`, `ds4_help.c`, `speed-bench/ab_bench.py`, `speed-bench/README.md`, `speed-bench/perf-record.md`, `docs/PERFORMANCE.md`, `tests/test_ab_bench.py`, `openspec/config.yaml` and this change's `openspec/` files; `docs/upstream-prs.md` is unchanged (no upstream commit adopted); `Makefile` is unchanged.
- [x] 8.6 Run `openspec validate 20-perf-bench-harness --strict` and confirm it passes.
